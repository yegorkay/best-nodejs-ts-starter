{
  "name": "nodejs-starter-yegorkay",
  "version": "1.0.0",
  "description": "The best node typescript starter period.",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index.ts",
    "build": "tsc --project ./",
    "check-types": "tsc",
    "check-format": "npm run prettier -- --list-different",
    "prettier": "prettier --ignore-path .gitignore \"**/*.+(js|json|jsx)\"",
    "format": "npm run prettier -- --write"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^14.14.22",
    "husky": "^4.3.8",
    "lint-staged": "^10.5.3",
    "nodemon": "^2.0.7",
    "prettier": "^2.2.1",
    "ts-node": "^9.1.1",
    "typescript": "^4.1.3"
  }
}
